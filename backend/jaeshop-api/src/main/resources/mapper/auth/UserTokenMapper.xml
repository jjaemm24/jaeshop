<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jaeshop.modules.auth.mapper.UserTokenMapper">

    <resultMap id="UserTokenMap" type="com.jaeshop.modules.auth.domain.UserToken">
        <id     property="id"           column="id"/>
        <result property="userId"       column="user_id"/>
        <result property="refreshToken" column="refresh_token"/>
        <result property="expiresAt"    column="expires_at"/>
        <result property="revoked"      column="revoked"/>
        <result property="createdAt"    column="created_at"/>
    </resultMap>

    <insert id="saveRefreshToken">
        INSERT INTO user_tokens (user_id, refresh_token, expires_at, revoked)
        VALUES (#{userId}, #{refreshToken}, #{expiresAt}, false)
    </insert>

    <select id="findByToken" resultMap="UserTokenMap">
        SELECT id, user_id, refresh_token, expires_at, revoked, created_at
        FROM user_tokens
        WHERE refresh_token = #{refreshToken}
        LIMIT 1
    </select>

    <update id="revokeToken">
        UPDATE user_tokens
        SET revoked = true
        WHERE refresh_token = #{refreshToken}
        AND revoked = false
    </update>

    <update id="revokeAllByUserId">
        UPDATE user_tokens
        SET revoked = true
        WHERE user_id = #{userId}
        AND revoked = false
    </update>

</mapper>

